package app;

public class BookApi {
    public boolean isBookAvailable(String bookName) {
        // Imagine this method interacts with an external book API
        // and checks if the specified book is available.
        // For simplicity, we'll just return true here.
        return true;
    }
}
